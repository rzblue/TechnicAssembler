language: java
install: 
    - printenv TEST_PASSWORD
    - cat test.txt
    - ./gradlew assemble
script: ./gradlew build
before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
